@{
    Layout = "_Layout";
}

<h2>Welcome, @User.Identity.Name!</h2>

<a href="@Url.Action("ChangePasswordStudent", "Student")" class="btn btn-warning">Thay đổi mật khẩu</a>

<form method="post" action="@Url.Action("Logout", "Auth")">
    <button type="submit">Logout</button>
</form>
