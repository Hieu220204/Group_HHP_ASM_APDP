@{
    ViewData["Title"] = "Student Home";
}

<h2>Welcome, Student</h2>

<p>Hello, @HttpContext.Session.GetString("username")</p>

<a href="@Url.Action("Logout", "Login")">
    <button>Logout</button>
</a>
